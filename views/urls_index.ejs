<!DOCTYPE html>
<html lang="en">
    <h1>Welcome to TinyApp</h1>
    <% if(!user_id){ %>
    <h2>Please login/register to start use this service!</h2>
    <div><% include partials/_header %></div>
    <% } %>
<body>
    <% if(user_id){ %>
      <% include urls_new  %>
      <% for(let user_link = 0; user_link < Object.keys(urls[user_id]).length; user++){ %>
        <ul>
            <p style="display: inline;">
               <a href="http://localhost:8080/u/<%= Object.keys(urls[user_id])[user_link] %>">http://localhost:8080/u/<%= Object.keys(urls[user_id])[user_link] %></a>
               ----> <%= Object.values(urls[user_id])[user_link] %>
               <button style="display: inline;" onclick="window.location.href = '/urls/<%= Object.keys(urls[user_id])[user_link] %>';">Update</button>
               <form method="POST" style="display: inline;" action="/urls/<%= Object.keys(urls[user_id])[user_link] %>?_method=DELETE">
                  <input type="submit" value="Delete">
               </form>
            </p>
        </ul>
      <% } %>
    <% } %>
</body>
</html>