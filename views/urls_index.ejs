<!DOCTYPE html>
<html lang="en">
    <% include partials/_header %>
<body>
    <% include urls_new  %>
    <% for(let i = 0; i < Object.keys(urls).length; i++) {%>
    <form method="POST" action="/urls/<%= Object.keys(urls)[i] %>/delete"><ul>
        <p>
           <a href='/urls/<%= Object.keys(urls)[i] %>'>
           <%= Object.keys(urls)[i] %></a>
           ----> <%= Object.values(urls)[i] %>
           <input type="submit" value="Delete">
        </p>
    </ul></form>
    <% }; %>
</body>
</html>